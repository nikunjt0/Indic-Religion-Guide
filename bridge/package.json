{
  "name": "indic-guide-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BlueBubbles iMessage bridge for the Indic Religion Guide RAG.",
  "scripts": {
    "start": "tsx --env-file=../.env src/index.ts",
    "dev": "tsx watch --env-file=../.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
